import { RedHighlightDirective } from './red-highlight.directive';

describe('RedHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new RedHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
